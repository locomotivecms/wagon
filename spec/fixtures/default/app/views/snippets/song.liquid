<li>
  <h3><a href="{% path_to song, with: a-song-template %}">{{ song.title }}</a></h3>
  <div class="cover">{{ song.cover | image_tag }}</div>
  <div class="info">
    {{ song.short_description }}
    <p class="listen">
      <a href="{{ song.audio_url }}">&rarr; Listen ({{ song.duration }} min)</a>
    </p>
    <p>écouter en Francais</p>
  </div>
  <div class="clear"></div>
</li>
